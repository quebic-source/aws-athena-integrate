config:
  aws:allowedAccountIds:
  - 893502944061
  aws:region: us-east-1
  project:apigateway:
    accessControlAllowHeaders: Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token,appid,appId,locationid
    accessControlAllowMethods: GET,POST,PUT,PATCH,DELETE,OPTIONS
    accessControlAllowOrigin: '*'
  project:apigatewayRoute:
    domain: VAR
    domainCertArn: VAR
    hostedZoneId: VAR
  project:auth:
    authorizedResources: /apps/{appId},
    authorizerFunc: authorizer-api
